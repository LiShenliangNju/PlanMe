# Planme 环境变量模板（复制为 .env 后填入本机值）
# pydantic-settings 会自动读取本文件；未列出的项沿用 .config/settings.py 默认值

# Ollama 本地模型服务
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b-instruct-q4_K_M

# 时区
TIMEZONE=Asia/Shanghai

# 说明：QQ 小号 / NapCat / 群白名单等扫描器专属配置仍放在
# .config/hmwk_scnr/config.yaml 中（含复杂列表，不适合放进 .env）
